/**
 * JS Datei für Browserseitige Logik
 * @author Tamino Fischer alias CodeKeks
 * */

function myProfileClicked(element){
    window.location.replace("profilesettings");
}

function mySupplementsClicked(element) {
    window.location.replace("/mysupplements");
}

function myTrainingsScheduleClicked(element) {
    window.location.replace("/scheduleoverview");
}